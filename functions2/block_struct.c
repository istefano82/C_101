#include "stdio.h"


#define square(x) x * x    /* WRONG */ 
//square(z+1)


#include "c_preproc.c"
#include "c_preproc.c"

int main()
{	
	int i = 100;
	int n = 20;
if (n > 0) {
    int i; /* declare a new i */
    for (i = 0; i < n; i++) printf("%d, \n", square(i +1));
}
i++;
}